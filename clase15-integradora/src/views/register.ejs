<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <style>
      body{
        margin: 0px;
        padding: 0px;
        background-color: #e9e9e9;
      }
      .card{
        max-width: 600px;
        background-color: white;
        margin: 110px auto 0px;
        border-radius: 10px;
        padding: 40px 37px 70px;
        box-shadow: 0 3px 6px rgba(0,0,0,0.16), 0 3px 6px rgba(0,0,0,0.23);
      }
      .input_field{
        display: flex;
        flex-direction: column;
        gap: 3px;
      }
      a, a:hover, a:active{
        text-decoration: none;
        color: black;
      }
    </style>
</head>
<body>
  <div class="container">
    <div class="card">
      <div>
        <h1>Registro</h1>
        <form action="/auth/register" method="POST" class="d-flex flex-column gap-2">
          <div class="input_field">
            <label for="nombre">Nombre</label>
            <input type="text" id="nombre" name="nombre" required>
          </div>
          <div class="input_field">
            <label for="apellido">Apellido</label>
            <input type="text" id="apellido" name="apellido" required>
          </div>
          <div class="input_field">
            <label for="email">Ingresa tu email</label>
            <input type="text" id="email" name="email" required>
          </div>
          <div class="input_field">
            <label for="password">Ingresa tu password</label>
            <input type="text" id="password" name="password" required>
          </div>
          <div class="input_field">
            <label for="edad">Edad</label>
            <input type="date" id="edad" name="edad" required>
          </div>
          <div>
            <button style="width: 100%; margin-top: 5px; cursor: pointer;" type="submit" class="btn btn-success">Registrate</button>
          </div>
        </form>
        <div style="display: flex; justify-content: space-between; margin-top: 8px;">
          <a href="/auth/login">Iniciar Sesión</a>
        </div>
      </div>
      <div>
        <!-- <button id="btnCookie">Obtener Cookies</button> -->
      </div>
    </div>
  </div>
  <script>
    let btn = document.getElementById("btnCookie");

    btn.addEventListener("click", async e => {
      console.log("Click!!!");

      const res = await fetch('/auth/cookies', {
        method: "GET",
        headers: {
          'Content-Type': 'application/json'
        }
      })
      console.log("Mi Respuesta es: ", (await res.json()));
    });

    
  </script>
</body>
</html>